# @interactive-os/json-document-rich-text-mention-react

Canonical React projection for `os.interactive/mention` Rich Text atoms.

```tsx
import { RichTextMentionAtom } from "@interactive-os/json-document-rich-text-mention-react";

<RichTextMentionAtom node={node} className="product-mention" />;
```
