import { tv } from "tailwind-variants";

export const richTextRecipe = tv({
  slots: {
    editor: "min-h-56 rounded-none border border-line-subtle bg-background-canvas p-5 text-base leading-7 text-foreground-strong outline-none",
  },
});
