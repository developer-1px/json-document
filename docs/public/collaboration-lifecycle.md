# Lifecycle

epoch는 기본 문서와 규칙, 누가 쓸 수 있는지가 고정되는 한 세대입니다.
브라우저 탭을 연 시간과 같지 않습니다.

checkpoint는 나중에 되돌리기에 충분한 보호된 기록입니다. 지금 진단 상태가
아닙니다. restore는 그 기록을 검증하고 runtime을 다시 만듭니다. 로컬 undo가
아닙니다. compaction은 긴 인과 기록을 새 epoch의 바탕으로 접습니다.

호출 순서와 저장 형식은 이어서 채웁니다.
